## Goal

윈도우에서 상단바(타이틀바)가 좁거나 클릭하기 불편한 창도, 조합 키 + 마우스 조작만으로 화면상 어느 위치를 눌러도 창을 이동시킬 수 있는 상주형 유틸리티의 개발 계획을 수립한다.

## Success Criteria

- 조합 키(기본값: `Win`)를 누른 상태에서 좌클릭 드래그하면, 커서 아래 창의 본문·버튼 영역을 눌렀어도 해당 최상위 창 전체가 따라 이동한다.
- 일반 클릭(조합 키 없음) 동작은 기존과 동일하게 유지된다. 포커스, 클릭, 드래그 선택 등 기존 앱 동작을 깨지 않는다.
- 최대화·전체화면 창 등 이동 불가 케이스는 안전하게 무시하고, 비정상 종료나 입력 먹통이 발생하지 않는다.
- 관리자 권한으로 실행된 창도 이동 대상에 포함한다. 이를 위해 AnyMove는 상승 권한으로 실행한다.
- 트레이 상주, 부팅 시 자동 시작 옵션, 조합 키 변경이 가능하다.

## Context And Current Facts

- 요청 배경: 일부 프로그램은 상단바를 잡기 불편하거나 위치에 따라 이동이 어렵다.
- 플랫폼은 윈도우로 한정된다. 프로그램 이름은 `AnyMove`로 확정했다.
- 워크스페이스(`E:\coding`) 확인 결과, 이 기능을 구현한 기존 코드는 확인되지 않았다. 신규 프로그램으로 기획한다.
- 기술 근거로 Win32 공식 문서를 직접 확인했다:
  - 전역 저수준 후크 설치는 `SetWindowsHookEx`(`WH_MOUSE_LL`, `WH_KEYBOARD_LL`)를 사용한다.
  - 커서 위치의 창 식별은 `WindowFromPoint`, 창 크기·위치 조회는 `GetWindowRect`, 실제 이동은 `SetWindowPos`를 사용한다.
  - 조합 키 상태 확인에는 `GetAsyncKeyState`를 사용할 수 있다.

## Constraints And Non-goals

- 제약:
  - Windows 데스크톱 전용. macOS/Linux 지원 없음.
  - 백그라운드 상주 + 전역 입력 후크 방식이므로 백신·보안 SW 오진 가능성을 고려해야 한다.
  - 상승 권한 실행을 전제로 한다. UAC 동의와 작업 스케줄러 기반 자동 시작이 필요하다.
  - 멀티모니터, DPI 스케일이 다른 환경을 고려해야 한다.
- Non-goals:
  - 창 크기 조절, 스냅, 가상 데스크톱 관리 등 이동 외 창 관리 기능.
  - 게임 오버레이 창의 강제 이동 우회.
  - 클라우드 동기화, 계정, 원격 제어.

## Key Decisions

1. 입력 가로채기 방식: `WH_MOUSE_LL` 저수준 마우스 후크 권장
   - 이유: 창 내부 어느 컨트롤을 눌러도 screen 좌표 기준으로 가로챌 수 있고, `SetWindowsHookEx` 문서가 데스크톱 앱의 전역 후크 용도를 명시한다.
   - 기각: 타이틀바 드래그 에뮬레이션(`WM_NCLBUTTONDOWN` 위조)만으로는 커스텀 타이틀바 앱 대응이 불안정하다.
   - 조합 키 판정은 마우스 후크 콜백 내 `GetAsyncKeyState` 확인 + 필요시 `WH_KEYBOARD_LL` 병행을 가정한다. 폴링 단독 방식은 반응성·CPU 측면에서 기각한다.

2. 이동 대상 창 판정: `WindowFromPoint` + 최상위 창으로 승격
   - 이유: 클릭 지점에서 얻는 핸들은 버튼·패널 같은 자식 창일 수 있어, 최상위(루트) 창으로 올려야 창 전체가 이동한다. 위치 기준은 `GetWindowRect`의 스크린 좌표를 사용한다.
   - 최대화·전체화면·고정 크기 창은 이동 시작 시 제외한다.

3. 이동 실행: `GetWindowRect`로 시작 위치 저장 → 마우스 이동 델타만큼 `SetWindowPos` 반복 호출
   - 이유: `SetWindowPos`는 위치·크기·Z-order 변경의 표준 API이며, `SWP_NOSIZE | SWP_NOZORDER | SWP_NOACTIVATE` 조합으로 크기·포커스 변경 없이 이동만 수행할 수 있다.
   - 드래그 중 원본 클릭 이벤트는 대상 앱으로 전달되지 않게 억제하는 것을 기본 방향으로 한다. 그렇지 않으면 버튼 오작동·텍스트 선택이 발생한다.

4. 구현 스택(확정): C# + .NET, Win32 API 직접 호출(P/Invoke)
   - 단일 실행 파일 게시, 트레이 아이콘, 설정 저장(`JSON`)을 갖춘 백그라운드 앱으로 구현한다.
   - 인스톨러는 Inno Setup으로 확정. 설치 시 작업 스케줄러 로그온 태스크(가장 높은 권한) 생성·제거를 담당한다.

5. 기본 UX(결정 반영):
   - 기본값 `Win + 좌버튼 드래그 = 이동`. `Win` 키는 앱 내부 충돌(`Ctrl` 복사, `Shift` 범위 선택)이 적어 선택했다.
   - 이동 모드에서는 시작 메뉴가 뜨지 않게 `Win` 키 입력을 억제한다. `Win+L` 등 OS 예약 조합 오조작 주의 문구를 제공한다.
   - 설정에서 `Alt` 등으로 조합 키 변경이 가능하다.
   - `Esc` 또는 버튼 릴리스로 이동 취소/종료.
   - 트레이 메뉴: 사용/중지 토글, 조합 키 변경, 자동 시작, 종료.

6. 상승 권한 실행(결정 반영):
   - 관리자 권한 창 이동을 위해 AnyMove 자체를 상승 권한으로 실행한다. 앱 매니페스트에 `requireAdministrator`를 선언한다.
   - 매일 UAC 프롬프트가 뜨지 않게, 자동 시작은 작업 스케줄러 로그온 트리거(가장 높은 권한 사용)로 등록하고 인스톨러가 이를 생성·제거한다.
   - UIAccess 방식(서명 인증서 필요)은 기각한다. 인증서 비용·관리 부담 대비 이점이 1차 범위에 필요하지 않다.

## Recommended Approach

1. 저수준 마우스 후크 스레드 + 메시지 펌프를 별도로 두고, `Win`이 눌린 상태의 좌버튼 다운만 이동 모드로 진입시킨다.
2. 진입 시점에 `WindowFromPoint`로 대상을 확정하고, `GetWindowRect`와 커서 위치의 오프셋을 저장한다.
3. `WM_MOUSEMOVE` 상당 이벤트마다 델타를 계산해 `SetWindowPos`로 이동시키고, 버튼 업에서 후크 억제를 해제한다.
4. 이동 불가 창(최대화, 전체화면, 특정 시스템 창)은 진입 전에 필터링한다. 관리자 권한 창은 상승 실행 덕분에 정상 이동 대상이다.
5. 설정·트레이·자동 시작은 이동 코어와 분리된 모듈로 구현해 후크 안정성에 영향을 주지 않게 한다.

## Work Plan

1. 입력 코어: `WH_MOUSE_LL` 설치/해제, `Win` 상태 판정, 좌버튼 다운·무브·업 처리, 이동 모드 상태머신 구현.
2. 창 판정: `WindowFromPoint` → 최상위 창 승격, 이동 가능 여부(최대화/전체화면/시스템 창) 필터 구현.
3. 이동 엔진: `GetWindowRect` 기준 오프셋 이동, `SetWindowPos` 호출(`SWP_NOSIZE | SWP_NOZORDER | SWP_NOACTIVATE`), 멀티모니터·DPI 경계 처리.
4. 클릭 억제 정책: 이동 중 원본 클릭이 앱에 전달되지 않게 처리하고, 이동 실패 시 폴백(일반 클릭 그대로 전달) 구현. 시작 메뉴 억제 포함.
5. 트레이·설정: 상주 아이콘, 사용/중지, 조합 키 변경, 자동 시작 등록, 설정 파일 저장/로드.
6. 패키징: 인스톨러 빌드. `requireAdministrator` 매니페스트, 작업 스케줄러 로그온 태스크(가장 높은 권한) 생성·제거, 미서명 실행 경고 문구 포함.

## Validation Plan

- 수동 E2E(핵심): 메모장·브라우저·탐색기에서 `Win + 본문 드래그`로 창이 오프셋 유지하며 이동하는지 확인. 일반 클릭이 정상 동작하는지 대조 확인. 시작 메뉴가 뜨지 않는지 확인.
- 관리자 권한 검증: 관리자 권한 메모장 등 상승 프로세스 창이 이동되는지 확인. 비상승 수동 실행 시 해당 창이 무시되는지 확인.
- 경계 케이스: 최대화 창, 전체화면 게임/영상, 멀티모니터+Y좌표 음수 영역, DPI 100%/150% 혼합에서 이동·무시 동작 확인.
- 최고 위험 검증: **이동 중 원본 클릭 억제 여부**. 실패하면 버튼 오클릭·드래그 선택이 발생하므로, 텍스트 에디터와 버튼이 많은 앱에서 우선 검증한다.
- 안정성: 장시간 상주(수 시간) 후 메모리·CPU 증가 없음, 후크 해제 후 입력 정상 복귀 확인.

## Risks / Rollback

- 상승 실행 부담: 매 수동 실행 시 UAC 동의가 필요하다. 자동 시작은 작업 스케줄러로 프롬프트 없이 처리하고, 수동 실행 빈도가 낮음을 전제로 한다.
- 백신 오진: 전역 후크는 키로거와 같은 API를 사용하므로 오진 가능성이 있다. 서명·배포 문서·동작 설명으로 완화한다.
- 포커스·입력 부작용: 클릭 억제 로직 오류 시 입력 먹통 가능. 후크 타임아웃, 예외 시 자동 해제, 킬 스위치(트레이 중지 + 프로세스 종료)를 둔다.
- `Win` 키 부작용: 시작 메뉴 억제 누락 시 메뉴가 뜨고, `Win+L` 등 오조작 시 화면 잠금 가능. 이동 모드 진입/종료 시 억제 범위를 명확히 하고 검증 계획에 포함한다.
- 롤백: 상주 프로세스 종료만으로 시스템이 원래 상태로 복귀한다. 인스톨러 제거 시 작업 스케줄러 태스크와 자동 시작 등록을 함께 제거한다.

## Open Questions

- 없음. 전 항목 결정됨(범위: 이동만, 배포: 인스톨러, 조합 키: `Win + 좌드래그`, 관리자 권한 창: 대응).

## Sources

- https://learn.microsoft.com/en-us/windows/win32/api/winuser/nf-winuser-setwindowshookexw
- https://learn.microsoft.com/en-us/windows/win32/api/winuser/nf-winuser-setwindowpos
- https://learn.microsoft.com/en-us/windows/win32/api/winuser/nf-winuser-windowfrompoint
- https://learn.microsoft.com/en-us/windows/win32/api/winuser/nf-winuser-getwindowrect
- https://learn.microsoft.com/en-us/windows/win32/api/winuser/nf-winuser-getasynckeystate
